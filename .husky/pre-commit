# 1) ESLint - fail on warnings or errors
npm run lint -- --max-warnings=0 || exit 1

# 2) Prettier - check formatting
npm run format:check || exit 1

# 3) TypeScript type-check without emitting files
npm run type-check || exit 1
